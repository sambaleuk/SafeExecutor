/**
 * @deprecated Import from '../adapters/sql/executor.js' instead.
 * This re-export exists for backward compatibility only.
 * The SQL executor has moved to src/adapters/sql/executor.ts as part of
 * the v2 restructure to support multi-domain adapters.
 */
export { executeSQLWithRollback as executeWithRollback } from '../adapters/sql/executor.js';
